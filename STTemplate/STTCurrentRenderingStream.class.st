"
I provide access to the current `WriteStream` where the `STTemplate` is being rendered.
"
Class {
	#name : #STTCurrentRenderingStream,
	#superclass : #DynamicVariable,
	#category : #'STTemplate-Core'
}
